// Write your function here
function removeElement(arr, discard) {
    let newArray = [];
    
    if(Array.isArray === false){
        return newArray;
        for(let i=0; i<array.length; i++){
            if(arr[i] === discard){
                return newArray;
            } return (arr[i] !== discard);
        }
    }
} 
let output = removeElement([1, 2, 3, 2, 1], 2); 
console.log(output);